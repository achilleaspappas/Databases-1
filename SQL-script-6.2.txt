SELECT ENAME "ΟΝΟΜΑ" FROM EMP WHERE 
	JOB IN ( SELECT JOB FROM EMP WHERE 
		DEPTNO = (SELECT DEPTNO FROM DEPT WHERE DNAME = 'ACCOUNTING'));
    

# ΟΝΟΜΑ
CODD
ELMASRI
DATE


SELECT ENAME "ΟΝΟΜΑ", SAL+IFNULL(COMM,0) "ΣΥΝΟΛΙΚΕΣ ΜΗΝΙΑΙΕΣ ΑΠΟΔΟΧΕΣ" FROM EMP A WHERE
	SAL+IFNULL(COMM,0) = (SELECT MAX(SAL+IFNULL(COMM,0)) FROM EMP WHERE 
		A.DEPTNO = DEPTNO);

# ΟΝΟΜΑ	ΣΥΝΟΛΙΚΕΣ ΜΗΝΙΑΙΕΣ ΑΠΟΔΟΧΕΣ
CODD	3000.00
NAVATHE	2000.00


SELECT ENAME "ΟΝΟΜΑ", SAL "ΜΙΣΘΟΣ" FROM EMP WHERE
	DEPTNO IN ( SELECT DEPTNO FROM DEPT WHERE DNAME = 'ACCOUNTING' )
	AND SAL < ( SELECT MAX(SAL) FROM EMP WHERE DEPTNO =
		( SELECT DEPTNO FROM DEPT WHERE DNAME = 'RESEARCH') );

# ΟΝΟΜΑ	ΜΙΣΘΟΣ
ELMASRI	1200.00
DATE	1800.00

